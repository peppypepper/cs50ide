#define Decksize 52 //much better way to handle "magic numbers" in your programs. Can't change decksize accidentally

card deal_cards(deck name)
{
    for(int i = 0; i < Decksize; i++)
    {
        // deal the card
    }
}